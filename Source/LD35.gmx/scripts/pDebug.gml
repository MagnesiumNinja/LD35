/*
    SCRIPT: pError(errorMessage);
    
    PARAMETERS:
        errorMessage - the error message to display to the console
        
    RETURNS:
        nothing
        
    AUTHOR: Chris Klassen
    
    DATE: April 11, 2015
    
    NOTES:
        This script prints an error message to the console.
*/

var msg;
msg = argument[0];

show_debug_message(msg);
